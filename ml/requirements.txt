# GPU box only (Lightning AI / Colab / HF Jobs). Not installed locally.
ultralytics>=8.3
# torch/torchvision come as ultralytics deps; on the GPU box install the CUDA
# build matching the machine, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
kaggle>=1.6
